# 当前研究主线

一句话目标：
- 解释 `NPS1/2/4` 与 `TP2/4/8` 组合下，vLLM CPU 推理性能变化的根因。

当前已知现象：
- `NPS2_TP4` 在核心吞吐指标上优于 `NPS1_TP2`。
- `NPS4_TP8` 出现明显退化，未复现浪潮“更优”的结论。

当前缺口：

- 现象存在，但缺少“源码执行路径 + 硬件计数器”联合支撑的闭环因果解释。
- 关键变量 `dtype`（当前多为 `bfloat16`）尚未完成与 `float16` 的系统对照。


# 当前任务清单
- [x] 结合 `vllm serve` CPU 执行路径与线程/NUMA 策略，分析`NPS1/2/4` 与 `TP2/4/8` 组合下，vLLM CPU 推理性能变化的根因。用户不太熟悉vllm的代码，需要讲解从vllm server入口开始，与TP和NUMA，线程，核心等相关的详细代码流程。用户的实验结果说明出不同的NPS，内存带宽等系统性能不同，但用户希望能深入了解究竟是什么原因导致了系统性能的变化。可新建文档保存这些信息便于用户后续查看。  
  交付文档：`info/vllm_cpu_nps_tp_root_cause_analysis.md`

