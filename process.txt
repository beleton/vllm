2026-04-14 19:38:05 +0800
- `wiki/`、`test_results/`、`tasks.json` 已承接当前主线结论、实验结果与待办；`process.txt` 只保留仍会影响后续复现、实验执行或问题排查的交接事项。

2026-04-09 14:50:23 +0800
- `cpu_attn_acc_locality` 的 `group_span` 唯一分片修正已完成；旧的“多个 subgroup 重复执行同一批 legacy slot”问题已不再是当前主线。
- 若继续验证 `acc-local-l3`，直接看 `wiki/源码分析/CPU_attention_acc_locality新kernel实现说明.md`、`wiki/实验结果解读/2026-04-05_Qwen3-30B-A3B_P3_AttnOnly_acc-local-l3对照观察.md` 和 `test_results/P3_AttnOnly/Qwen3-30B-A3B/NPS1_TP2/prefill-like/global-fixed/batch_16/summary.md`。
- `q128_kv128/acc-local-l3_span4` 当前仍缺 PCM 报告；复用该结果目录时，不要把该点当成完整 PCM 对照。

2026-04-03 15:02:27 +0800
- 运行 `python setup.py build_ext --inplace`、`pytest`、或任何依赖 `torch.ops._C*` 的命令时，先 `conda activate vllm-cpu`，并显式带 `PYTHONNOUSERSITE=1`，避免误加载 `~/.local/lib/python3.12/site-packages/torch`。

2026-04-02 09:15:24 +0800
- `attention-only + AMDuProfPcm` 口径下，benchmark 自身的 `summary.json` 可能不落盘；整理结果时优先使用 `dry_run_summary.json + report.json/report-cumulative.csv`。

2026-04-01 12:10:00 +0800
- CPU `TP=2` 场景下，`AMDuProfCLI hotspots` 的 launcher 模式会被 `spawn` 路径打断；正式口径改为先启动目标，再按 `Worker_TP* pid` 用 `AMDuProfCLI collect -p <worker_pids>` attach。
- `IBS L3-miss` 当前仍可保留 launcher 模式；仅在复现出新的兼容性问题时再调整。

2026-04-01 00:02:04 +0800
- 若 `strict-batch` 在 CPU `TP=2` 场景报 `create shm in SHMManager failed. errno: 17`，先按端口前缀检查 `/dev/shm/<port>-tp:0-[0-1]-cpushm_*`。
- 确认是孤儿文件后，只删除对应端口那一对 `cpushm` 文件，不要清空整个 `/dev/shm`。

2026-03-30 22:48:30 +0800
- `AMDuProfPcm` 在 TP 场景下即使已生成 `report-timeseries.csv/report-cumulative.csv`，launcher 也可能不自动退出；`-d` 只能当采样上界，不能当正常收尾机制。
- 若报告已落盘但命令不退，先发 `SIGTERM` 或 `SIGINT`，等待补齐 `session.uprof/report.html`；仅在温和收尾无效时再 `kill -9`。

2026-04-02 17:19:41 +0800
- `IBS` 来源级平均延迟口径依赖 `collect` 时显式带 `-e event=ibs-op,ibsop-l3miss=1,call-graph`。
- 若后续升级 `AMDuProf` 版本，先复核 `AMDuProf_Collect_Help.txt`、`ibs_op_ld.json`、`ibs_op_ld_lat.json`、`0x1a_0x1.xml` 中的定义，再复用现有公式。
