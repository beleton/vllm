{
    "project": "Chiplet CPU推理加速",
    "updated_at": "2026-05-07 15:10:00 +0800",
    "tasks": [
        {
            "id": "9",
            "description": "补跑 `prefill_B16_I256_O1`，并补齐 prefill 长度 sweep 以定位 L3 行为转折区间",
            "status": "pending",
            "passes": false
        },
        {
            "id": "10",
            "description": "基于已落地的 attention-only 多进程 TP benchmark，用 `AMDuProfPcm` 补齐 `NPS1_TP2/NPS2_TP4/NPS4_TP8` 的 `global-fixed/per-rank-fixed` 对照，判断 attention kernel 的 locality 优化空间",
            "status": "pending",
            "passes": false
        },
        {
            "id": "228",
            "description": "在固定 `NPS1_TP2/global-fixed` 拓扑下补更密长度点，优先覆盖 `prefill q=512 -> 1024` 与 `decode kv=1024 -> 2048`，继续验证 locality mode 切换边界",
            "status": "pending",
            "passes": false
        },
        {
            "id": "35",
            "description": "已实现 `attention-only` 多进程 TP benchmark，复用 `CPUWorker.init_device()` 的 NUMA/OMP 绑核逻辑，支持 `global-fixed/per-rank-fixed` 与统一 `decode-like/prefill-like` 输入路径",
            "status": "completed",
            "passes": true
        },
        {
            "id": "149",
            "description": "已修正 `cpu_attn_acc_locality` 的 `group_span` 唯一分片执行，避免多 subgroup 重复执行同一 `kv_head` 的 legacy slot / reduction item",
            "status": "completed",
            "passes": true
        },
        {
            "id": "248",
            "description": "`tools/p3_attn_only/pcm_compare.py` 已纳入 `IPC/CPI (Sys + User/Sys/User)` 汇总输出，并补回归测试",
            "status": "completed",
            "passes": true
        },
        {
            "id": "255",
            "description": "已新增 `tools/p3_attn_only/pcm_l3_source_latency_compare.py`，用于汇总 `pcm_l3_source_latency_raw` 的 `report-cumulative.csv` 并生成对照 CSV",
            "status": "completed",
            "passes": true
        },
        {
            "id": "260",
            "description": "已修复 `test_results/amduprof_pcm_l3_source_latency.conf` 中 `L3 Access/Miss (pti)` 的失真，改用官方 `$RetdInstL3` 口径并补回归测试",
            "status": "completed",
            "passes": true
        },
        {
            "id": "340",
            "description": "已改造 CPU attention profiling 口径：程序内累计 scheduler/runtime profile，benchmark 仅统计正式迭代，并在 JSON 输出 rank 级与汇总 profile",
            "status": "completed",
            "passes": true
        },
        {
            "id": "341",
            "description": "`tools/p3_attn_only/pcm_compare.py` 已汇总 `profile.json` 中 scheduler/runtime/slowest-rank 并行度指标，并写入 `summary.csv` 等汇总文件",
            "status": "completed",
            "passes": true
        },
        {
            "id": "345",
            "description": "`L3 source latency` raw 口径已改为来源 request share，并调整 `Local Memory` 与 `another CCX in same node` 的指标顺序",
            "status": "completed",
            "passes": true
        },
        {
            "id": "347",
            "description": "已新增 `wiki/实验方案/Qwen3-30B-A3B_attention-only_L3驻留验证实验步骤.md`，整理 `PCM/resctrl/CAT/IBS` 命令验证 `balanced/acc-local-l3` 的本地 `L3` 驻留与 `K/V` 复用",
            "status": "completed",
            "passes": true
        },
        {
            "id": "348",
            "description": "已新增 `tools/p3_attn_only/pcm_l3_task_metrics.py` 计算 `L3 Miss / attention task`，L3 驻留方案以自定义 `pcm_l3_source_latency` 为主口径",
            "status": "completed",
            "passes": true
        },
        {
            "id": "350",
            "description": "两份 attention-only 实验步骤已加入 `cleanup_vllm_all`，在启动 `benchmark_cpu_attn_mp.py` 前先清空残留 `vllm/multiprocessing` 进程",
            "status": "completed",
            "passes": true
        },
        {
            "id": "351",
            "description": "`tools/p3_attn_only/pcm_l3_task_metrics.py` 已兼容纯 PCM 结果目录，不再依赖 `profile.json` 发现 case；缺失 profile 时保留可计算字段并标注 per-task 指标不可用",
            "status": "completed",
            "passes": true
        },
        {
            "id": "352",
            "description": "复核 `benchmark_cpu_attn_mp.py` 相对 `benchmark_cpu_attn.py` 的实现差异，确认会影响 CPU attention 性能测试解释的代码路径与实验噪声来源",
            "status": "completed",
            "passes": true
        },
        {
            "id": "353",
            "description": "已完善 `wiki/资料总览/汇报.md`，整理 attention ACC 实现思路、实测性能和理论差距",
            "status": "completed",
            "passes": true
        },
        {
            "id": "354",
            "description": "已在 `wiki/资料总览/汇报.md` 补充下一步计划：按 L3 驻留验证实验确认 `K/V` 是否留在本地 `L3`",
            "status": "completed",
            "passes": true
        },
        {
            "id": "355",
            "description": "已将 `benchmark_cpu_attn_mp.py` 改为仅共享 `block_table_seed`，保证同一 case 内各 TP rank 的随机 `block_table` 逻辑一致，同时保留 `query/key/value` 数据 seed 随 rank 变化；`block_table` 行内随机改为非放回并排除保留的 `block_id=0`，补齐 worker benchmark 路径的 `torch.inference_mode()`",
            "status": "completed",
            "passes": true
        },
        {
            "id": "356",
            "description": "已分析 P4_AttnOnly_L3Residency 实验一 prefill-like 数据，补充 `pcm_l3_task_metrics.py` 汇总标准 PCM 的 IPC/L2/L3/DC 指标，并撰写实验分析文档，结论为跨CCD访问不是当前 CPU attention 主瓶颈",
            "status": "completed",
            "passes": true
        },
        {
            "id": "357",
            "description": "已整理 CPU attention L3局部性阶段报告，并更新 L3驻留验证实验步骤：将 `resctrl llc_occupancy` 降为可选实验，确认后续最小补证据集合为 `CAT(q8192/q16384)` 与 `IBS(q8192)`",
            "status": "completed",
            "passes": true
        },
        {
            "id": "358",
            "description": "已重写 `Qwen3-30B-A3B_attention-only_L3驻留验证实验步骤.md`，压缩表述并新增 `group_span=8` 诊断定位",
            "status": "completed",
            "passes": true
        },
        {
            "id": "359",
            "description": "已将 P4_AttnOnly_L3Residency 结果目录统一为 `batch_x/spanY/q*_kv*/mode`，补齐 `pcm_l3_task_metrics.py` 对 `span` 布局的兼容，并更新实验文档到 `span` 命名",
            "status": "completed",
            "passes": true
        },
        {
            "id": "360",
            "description": "已新增 `tools/p3_attn_only/cat_compare.py`，汇总 `spanY` baseline 与 `spanY/cat/*/{mask}` 的 `dry_run_summary.json`，输出 `cat_compare_summary.csv` 供 `0001/ffff` 同口径对比",
            "status": "completed",
            "passes": true
        },
        {
            "id": "361",
            "description": "已新增 `wiki/实验结果解读/2026-05-06_Qwen3-30B-A3B_CPU_Attention_ACC局部性优化结论.md`，基于 `P3/P4` 与 CAT 结果说明 `acc-local-l3` 不适合作为当前 attention 优化主线",
            "status": "completed",
            "passes": true
        },
        {
            "id": "362",
            "description": "已在 `wiki/实验方案/Qwen3-30B-A3B_attention-only_L3驻留验证实验步骤.md` 补充 `resctrl llc_occupancy` 单点旁证实验命令",
            "status": "completed",
            "passes": true
        },
        {
            "id": "363",
            "description": "已将 `resctrl llc_occupancy` 实验命令改为固定 `--iters 150` 口径，并补回 `occupancy_summary.json` 输出",
            "status": "completed",
            "passes": true
        },
        {
            "id": "364",
            "description": "已扩展 `resctrl llc_occupancy` 实验命令支持 `mask` 参数，`0001` 输出写入独立目录以避免覆盖已有 `ffff` 结果",
            "status": "completed",
            "passes": true
        },
        {
            "id": "365",
            "description": "已将 `resctrl llc_occupancy` 的 `ffff` 输出路径统一为 `${locality_mode}/ffff/span1`",
            "status": "completed",
            "passes": true
        },
        {
            "id": "366",
            "description": "已将磁盘上旧的 `resctrl llc_occupancy` ffff 结果从 `${locality_mode}/span1` 迁移到 `${locality_mode}/ffff/span1`",
            "status": "completed",
            "passes": true
        },
        {
            "id": "367",
            "description": "已新增 `tools/attn_only/llc_occupancy_compare.py`，用于汇总 `resctrl llc_occupancy` 的 `ffff` 与 `0001` 结果并输出对比 CSV",
            "status": "completed",
            "passes": true
        },
        {
            "id": "368",
            "description": "`tools/attn_only/llc_occupancy_compare.py` 已改为按 MB 输出 16 个 `L3` 与 `Total` 的 mean/peak 柱状图，并去掉 `total_nonzero_mean_mb` 等不需要的汇总列",
            "status": "completed",
            "passes": true
        },
        {
            "id": "369",
            "description": "`tools/attn_only/llc_occupancy_compare.py` 已改为单页 HTML 报告，支持 `ffff/0001` 对比 `slowest_rank_mean_ms`，并按长度展示 `balanced/acc-local-l3` 的 L3 与 Total 占用",
            "status": "completed",
            "passes": true
        }
    ]
}
