# ACC-local-L3 尝试完整分析

> 本文档是对 ACC-local-L3 方向的完整分析记录，包含研究思路、实现、策略验证、性能数据和最终结论。
> ACC 最终被判定为不可行，但分析过程中的多项发现（attention 主要瓶颈不是跨 CCD 访问、L3 驻留数据不在关键路径上、locality 改善不必然带来时延收益）对后续方向有参考价值。

## 研究目标

当前工作关注 vLLM CPU attention 在 Chiplet CPU 上的 `K/V` 访问局部性。Chiplet CPU 的核心被分布在多个 `CCD/CCX` 上，每个局部域有独立的本地 `L3`。如果同一份 `K/V` 被多个 `CCD` 反复读取，会产生缓存副本、跨 `CCD` 补线和本地 `L3` 容量竞争。

ACC（Attention Computation Cluster，注意力计算集群）在这里指共享同一组 `K/V` 的 attention 任务集合。当前实现的目标是把这些任务尽量限制在同一个本地 `L3` 局部域内执行，减少同一 `K/V` 在多个 `CCD` 之间扩散。

## CPU attention 的任务结构

CPU attention 不会一次性把整个 attention 矩阵算完，而是先切成多个 `workitem`。每个线程领取一个任务后，会在任务内部继续切分 `q_tile` 和 `kv_tile`，再执行 attention 计算。

直观流程如下：

```text
for each workitem:
  确定本次要计算的 kv_head 和 q 范围
  for each q_tile:
    for each kv_tile:
      计算当前 q_tile 对当前 kv_tile 的 attention
```

`q_tile` 是 query 方向的小块，`kv_tile` 是当前可见 `K/V` 前缀的小块。在 prefill（提示预填充）场景下，后续 `q_tile` 会继续访问前面的 `K/V` 前缀。因此，同一个 `workitem` 内会多次访问相同或重叠的 `K/V` 数据。如果这些 `K/V` 能留在本地 `L3`，后续访问可以从本地缓存获得；如果被挤出，则需要从其他 `CCD` 或内存重新获取。

## balanced 调度的问题

vLLM 原有 `balanced` 策略采用全局抢任务方式。所有线程共享一个任务池，谁先空闲就领取下一个任务。这种策略优先保证线程负载均衡，但不保证 `kv_head` 与本地 `L3` 的绑定关系。

其结果是：

- 同一个 `kv_head` 的任务可能被多个 `CCD` 上的线程计算。
- 同一个 `CCD` 可能同时处理多个不同 `kv_head`。
- 多个 `kv_head` 的 `K/V` 同时进入同一个本地 `L3` 后，会增加容量竞争。
- 当单个 `kv_head` 的 `K/V` 仍可能留在 `L3`，但多个 `kv_head` 的聚合工作集超过本地 `L3` 时，`balanced` 更容易丢失 `K/V` 复用机会。

## ACC-local-L3 的实现思路

当前 `acc-local-l3` 没有改变 attention 的数学计算过程，也没有重写 tile 计算、softmax 或 reduction。它主要改变任务领取范围。

实现思路分三步：

1. 先按硬件局部性把线程分组。一个分组对应一个本地 `L3/CCD` 局部域。
2. 再把每个 `kv_head` 分配给一个或多个连续分组。
3. 线程只能领取自己所在分组覆盖到的 `kv_head` 任务。

简化后的对比如下：

```text
balanced:
  所有线程共享一个全局任务池
  任意线程都可以领取任意 kv_head 的任务

acc-local-l3:
  线程先归属到本地 L3 分组
  每个 kv_head 只开放给指定分组
  分组内部线程动态领取该 kv_head 的任务
```

`acc-local-l3` 不是静态地把每个任务固定给某个线程。它仍保留分组内部的动态领取机制：同一个 `kv_head` 的任务由覆盖它的本地线程池共同领取，避免同一任务被重复执行，同时把 `K/V` 访问范围限制在更小的本地 `L3` 范围内。

在当前常用的 `qhead=32 / kvhead=16 / TP=2 / group_span=1` 实验配置下，每个 rank 有 `8` 个本地 `kv_head`，机器上每个 rank 可见 `8` 个本地 `L3` 分组。因此可近似理解为：每个本地 `kv_head` 固定到一个本地 `L3` 分组上执行。

## 策略生效验证

`q256` 的运行时日志确认策略已经生效：

- `balanced`：单个 `kv_head` 最多横跨 `5` 个 `CCD`，单个 `CCD` 最多同时计算 `7` 个 `kv_head`。
- `acc-local-l3`：每个 `kv_head` 只落在 `1` 个 `CCD`，每个 `CCD` 只计算 `1` 个 `kv_head`。

任务收敛到单 CCD 的机制已正确执行。

## 理论预期

以 `head_dim=128`、`bf16` 为例，单个 `kv_head` 的 `K+V` 数据量为：

```text
K + V = 2 × 128 × 2 B = 512 B / token
```

一个约 `32 MiB` 的本地 `L3` 理论上可容纳约 `65,536` token 的单 `kv_head` K/V。实际可用容量会低于该值，因为还存在 `Q`、中间结果、元数据、scratchpad、其他线程数据和缓存碎片。

理论上，`acc-local-l3` 的收益窗口出现在以下条件同时成立时：

- 同一个 `kv_head` 的 `K/V` 会被多个 `q_tile` 重复访问。
- 单个 `kv_head` 的 `K/V` 仍有机会在本地 `L3` 中复用。
- 多个 `kv_head` 聚合后会造成同一 `L3` 容量竞争。
- 限制 `kv_head` 的执行范围不会明显降低线程并行度。

在这些条件下，`acc-local-l3` 应减少跨 `CCD` 扩散和本地 `L3` 争用，从而降低 attention 时延。

## 性能结果

实验条件：`NPS1_TP2 / prefill-like / global-fixed / batch=1 / qhead=32 / kvhead=16 / group_span=1`。

| q=kv | balanced slowest rank mean (ms) | acc-local-l3 slowest rank mean (ms) | acc 相对变化 |
| ---: | ---: | ---: | ---: |
| 64 | 0.028 | 0.029 | +4.51% |
| 128 | 0.029 | 0.029 | +1.26% |
| 256 | 0.055 | 0.054 | -3.09% |
| 512 | 0.119 | 0.118 | -0.96% |
| 1024 | 0.374 | 0.371 | -0.62% |
| 2048 | 1.414 | 1.873 | +32.49% |
| 4096 | 6.631 | 7.798 | +17.61% |
| 8192 | 29.634 | 32.847 | +10.84% |
| 16384 | 126.621 | 138.038 | +9.02% |
| 32768 | 516.573 | 564.512 | +9.28% |
| 65536 | 2112.045 | 2251.869 | +6.62% |

- 短序列 `q256/q512/q1024` 有小幅收益，最大为 `3.09%`。
- `q2048+` 的主区间持续退化，范围 `+6.62% ~ +32.49%`。
- 同一批数据中，`acc-local-l3` 能降低 `L3 Access (pti)` 和跨 `CCD` demand fill，但这些改善没有稳定转化为端到端时延收益。

## 有效并行度

长序列下 `acc-local-l3` 的有效并行度下降。以最慢 rank 的 attention 有效线程数为例：

| q=kv | balanced | acc-local-l3 |
| ---: | ---: | ---: |
| 2048 | 97.43 | 76.03 |
| 4096 | 86.85 | 74.11 |
| 8192 | 78.95 | 71.27 |
| 16384 | 77.57 | 71.63 |

## L3 指标与性能脱钩

`acc-local-l3` 在长序列上显著降低了 `L3 Miss / attention task`，但变化没有落到最终性能。

| q=kv | runtime 变化 | balanced L3 Miss/task | acc L3 Miss/task | L3 Miss/task 变化 |
| ---: | ---: | ---: | ---: | ---: |
| 4096 | +18.70% | 10254.83 | 9464.04 | -7.71% |
| 8192 | +11.39% | 51426.87 | 24137.74 | -53.06% |
| 16384 | +9.77% | 268554.52 | 153566.39 | -42.82% |
| 32768 | +9.09% | 2127041.35 | 1539152.42 | -27.64% |
| 65536 | +8.21% | 10923086.67 | 9045469.04 | -17.19% |

`L3 Miss / task` 大幅下降（`q8192` 降 `53%`），runtime 仍然退化。L3 miss 的减少没有触及性能关键路径。

## 跨 CCD 访问占比

标准 PCM 显示，当前 attention 的 demand fill 主体是本地 `L2`，跨 `CCD` demand fill 份额很小。

| q=kv | mode | All Demand DC Fills (pti) | Local L2 (pti) | another CCX same node (pti) | remote share |
| ---: | --- | ---: | ---: | ---: | ---: |
| 2048 | balanced | 8.62 | 8.32 | 0.07 | 0.81% |
| 2048 | acc-local-l3 | 7.90 | 7.64 | 0.05 | 0.63% |
| 4096 | balanced | 7.88 | 7.68 | 0.03 | 0.38% |
| 4096 | acc-local-l3 | 7.35 | 7.16 | 0.03 | 0.41% |
| 8192 | balanced | 7.25 | 7.10 | 0.01 | 0.14% |
| 8192 | acc-local-l3 | 6.85 | 6.70 | 0.01 | 0.15% |
| 16384 | balanced | 6.80 | 6.68 | 0.01 | 0.15% |
| 16384 | acc-local-l3 | 6.67 | 6.55 | 0.01 | 0.15% |

`acc-local-l3` 的设计目标之一是降低 `K/V` 副本扩散。当前数据中，跨 `CCD` demand fill 仅为 `0.14%~0.81%`，不是主要访问来源。继续压低该项的收益上限很小。

## CAT 容量限制实验

CAT 实验使用 `resctrl` 控制每个本地 `L3/CCD` 的可用 cache mask。`ffff` 是全开（不限制），`0001` 将每个本地 `L3` 的可用容量压缩到约 `1/16`（极端限制）。若本地 `L3` 容量和 `K/V` 副本冗余是当前 attention 的主性能杠杆，`0001` 应在长序列上造成显著退化。

### runtime 变化

| q=kv | mode | ffff slowest rank mean (ms) | 0001 slowest rank mean (ms) | 0001 vs ffff |
| ---: | --- | ---: | ---: | ---: |
| 1024 | balanced | 0.381 | 0.415 | +8.80% |
| 1024 | acc-local-l3 | 0.382 | 0.387 | +1.45% |
| 4096 | balanced | 6.742 | 7.099 | +5.29% |
| 4096 | acc-local-l3 | 7.944 | 8.074 | +1.64% |
| 8192 | balanced | 29.935 | 30.566 | +2.11% |
| 8192 | acc-local-l3 | 33.169 | 33.687 | +1.56% |
| 16384 | balanced | 128.106 | 128.173 | +0.05% |
| 16384 | acc-local-l3 | 139.473 | 140.174 | +0.50% |
| 32768 | balanced | 521.562 | 522.888 | +0.25% |
| 32768 | acc-local-l3 | 567.273 | 567.907 | +0.11% |
| 65536 | balanced | 2113.140 | 2115.858 | +0.13% |
| 65536 | acc-local-l3 | 2285.081 | 2285.782 | +0.03% |

**`0001` 对 `q8192+` 的影响只有 `0.03%~2.11%`**，短序列 `q1024/q4096` 相对更敏感（`+1.45%~+8.80%`），但短序列的 runtime 绝对值很小且不在主优化区间。长序列对本地 `L3` 容量限制不敏感。

### resctrl L3 occupancy（16 个 L3 domain 合计）

| q=kv | mode | ffff 总均值 (MB) | ffff 总峰值 (MB) | 0001 总均值 (MB) | 0001 总峰值 (MB) |
| ---: | --- | ---: | ---: | ---: | ---: |
| 1024 | balanced | 72.3 | 249.1 | 6.2 | 26.3 |
| 1024 | acc-local-l3 | 72.5 | 249.2 | 6.0 | 25.0 |
| 4096 | balanced | 97.6 | 411.8 | 7.5 | 29.9 |
| 4096 | acc-local-l3 | 85.1 | 278.3 | 7.8 | 27.7 |
| 8192 | balanced | 134.4 | 468.1 | 8.9 | 30.1 |
| 8192 | acc-local-l3 | 119.9 | 366.7 | 9.2 | 28.6 |
| 16384 | balanced | 246.7 | 475.5 | 15.0 | 29.9 |
| 16384 | acc-local-l3 | 221.4 | 412.9 | 14.3 | 27.3 |
| 32768 | balanced | 339.7 | 513.4 | 22.1 | 32.6 |
| 32768 | acc-local-l3 | 267.8 | 406.7 | 19.2 | 27.1 |
| 65536 | balanced | 370.0 | 496.8 | 26.3 | 32.4 |
| 65536 | acc-local-l3 | 251.8 | 383.7 | 21.6 | 26.6 |

观察：

- `ffff` 下，长序列的 L3 occupancy 峰值已接近 16 个 CCD 总理论容量（约 `512 MB`）。`acc-local-l3` 的 occupancy 均值略低于 `balanced`（如 `q65536`：`251.8 MB` vs `370.0 MB`），说明任务收敛策略确实改变了 L3 驻留形态。
- `0001` 将可用容量压缩到约 `1/16`，总 occupancy 被严格压缩到均值 `6~26 MB`、峰值 `25~33 MB`。该约束生效，但 runtime 几乎不受影响。

### CAT 结论

`q8192+` 在 `0001` 下只退化 `0.03%~2.11%`，但 `ffff` 下的 occupancy 数据显示长序列已经形成大量稳定的 L3 驻留（`q65536` 均值 `240~370 MB`）。CAT 将驻留量压缩到约 `1/16`，runtime 几乎没变。这明确说明：

- 那些在 `ffff` 下驻留在 L3 中的数据（主要是 `K/V` tile 和中间结果）不在主要性能关键路径上。
- 数据以流式扫描、预取、短生命周期 cache line 为主。L3 容量被压缩后，这些 cache line 由本地 L2 和预取机制承接，没有显著增加关键路径等待。

attention 运行期间确实形成了大量 L3 驻留，但这些驻留数据不重要。本地 L3 副本冗余的优化，作用对象是这些不重要的驻留数据。

## 理论与实测差距

理论分析主要抓住了 `K/V` 复用和本地 `L3` 容量两个因素，假设把同一 `kv_head` 限制在同一个本地 `L3` 后，减少的数据搬移能直接转化为时延收益。实测表明这个假设不完整。

当前差距主要来自三点：

1. `acc-local-l3` 限制了跨分组抢任务能力。`balanced` 可以让空闲线程继续领取其他 `kv_head` 的任务，而 `acc-local-l3` 只能在本地分组覆盖范围内补空。长序列下单个任务成本更不均匀时，这会降低有效并行度。
2. 局部性收益和并行度损失同时存在。`acc-local-l3` 可以减少部分跨 `CCD` 访问，但如果线程等待、负载不均或局部任务池耗尽带来的损失更大，最终时延仍会变慢。
3. 长序列下瓶颈不再只由本地 `L3` 决定。随着 `K/V` 工作集继续增大，访问来源会更多转向本地内存，单纯把 `kv_head` 收紧到一个 `L3` 分组内，不能保证继续获得收益。

## 结论

1. **cache 行为改善没有转成 runtime 收益。** `q8192+` 的 `L3 Miss / task` 下降 `17.19%~53.06%`，L3 occupancy 均值降低 `23%~32%`（`q65536 acc-local-l3: 251.8 MB` vs `balanced: 370.0 MB`），runtime 仍退化 `8.21%~11.39%`。

2. **目标瓶颈不成立。** 跨 `CCD` demand fill 在 `q2048+` 仅约 `0.14%~0.81%`，主体 demand fill 来自本地 `L2`。减少跨 CCD 扩散的收益空间本身很小。

3. **容量假设不成立。** 极端 CAT `0001` 下 L3 occupancy 被压缩到 `ffff` 的约 `1/16`（均值从 `240~370 MB` 降到 `6~26 MB`），但 `q8192+` 只退化 `0.03%~2.11%`。attention 对本地 `L3` 容量限制不敏感，那些在 `ffff` 下驻留的大量数据不在关键路径上。

综上，**ACC-local-L3 方向被判定为不可行。** 核心原因是其依赖的两个假设——跨 CCD 访问是主要瓶颈、本地 L3 容量竞争影响性能——在实测中均不成立。
